# IDE settings
.idea/
*.iml
.gradle
/local.properties
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/admin-creator/
/evanta-99990-firebase-adminsdk-fbsvc-4863037e02.json

# Supabase CLI temporary files & local secrets
supabase/.temp/
supabase/.env*

# Release signing — never commit the keystore or its passwords.
keystore.properties
*.jks
*.keystore

# Build outputs — don't commit compiled apps (attach them to a GitHub Release instead).
/app/release/
*.apk
*.aab

# Junk from the Linux/FUSE mount.
.fuse_hidden*
